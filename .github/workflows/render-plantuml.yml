name: render-plantuml

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  render-plantuml:
    name: Render PlantUML to wiki
    uses: frederikheld/render-plantuml-to-wiki-action@v1.0.1
    with:
      WIKI_TOKEN: ${{ secrets.WIKI_TOKEN }}
      INPUT_DIR: "../../plantuml_input_dir"
      OUTPUT_DIR: "../../plantuml_output_dir"
